nav
    .list-container
        ul
            each item in categoryItems
                li: a(href='/items/' + item._id)= item.name
    .btn-group
        button.log--open #[span >] PUSH
        button(onclick='location.assign("/items")') #[span <] BACK
