nav
    .list-container
        ul
            each category in categories
                li: a(href='#' + category)= category
    .btn-group
        button.logs--open #[span L]
        button.new--open #[span +]
        button.new-multi--open #[span *]
